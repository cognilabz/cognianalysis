# Codex Harness

Use the Cognianalysis skill when available.

Run `cognianalysis analyze .`, open `.analysis/TASK.md`, execute the listed workpacks, write shard JSON files to their exact paths, then write `.analysis/analysis.json`.

Keep production source files read-only unless the user explicitly asks for code changes. The CLI validates evidence and renders; Codex authors semantic findings.

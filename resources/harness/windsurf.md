# Windsurf Harness

Prepare the workspace with `cognianalysis analyze .`, then follow `.analysis/TASK.md`.

After `00-planner`, independent shard workpacks may be run in parallel. Preserve write locks from `.analysis/workpack-manifest.json`.

Every major claim in shards or `.analysis/analysis.json` needs file:line evidence or an explicit evidence gap/open question.
